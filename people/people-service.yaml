apiVersion: v1
kind: Service
metadata:
    name: people-service
spec:
    type: ClusterIP
    selector:
        app: people-service
    ports:
        - protocol: TCP
          port: 80
          targetPort: 7802
