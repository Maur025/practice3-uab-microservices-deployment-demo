apiVersion: v1
kind: Service
metadata:
    name: purchase-service
spec:
    type: ClusterIP
    selector:
        app: purchase-service
    ports:
        - protocol: TCP
          port: 80
          targetPort: 7801
